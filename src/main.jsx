import React from "react";
import ReactDOM from "react-dom/client";
import AIPricingCalc from "../saas-breakeven-calculator.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <AIPricingCalc />
  </React.StrictMode>
);
